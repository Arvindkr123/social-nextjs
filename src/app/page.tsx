import React from "react";
import {
  ClerkProvider,
  SignInButton,
  SignedIn,
  SignedOut,
  UserButton,
} from "@clerk/nextjs";
import { ModeToggle } from "@/components/ModeToggle";

const Home = () => {
  return (
    <div>
      <h2>Home page cotent</h2>
    </div>
  );
};

export default Home;
